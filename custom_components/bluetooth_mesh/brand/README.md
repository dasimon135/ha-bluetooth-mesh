# Brand assets

Home Assistant (>= 2026.3) loads custom integration icons from this `brand/`
directory instead of a PR to the `home-assistant/brands` repository.

Add `icon.png` (and optionally `dark_icon.png`, `logo.png`) here in a later
task. No binary assets are committed yet.
